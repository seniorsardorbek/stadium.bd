export enum OrderStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  CANCELED = "canceled",
}

export enum ItemType {
  FOOD = "food",
  GOOD = "good",
}

export enum SortOrder {
  ASC = "asc",
  DESC = "desc",
}

export enum UserRole {
  ADMIN = "admin",
  PLAYER = "player",
  OWNER = "owner",
}
